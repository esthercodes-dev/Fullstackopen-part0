```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: User types a new note and clicks Save

    browser->>browser: JS intercepts form submission
    browser->>browser: JS creates new note object
    Note right of browser: JS prepares note for server

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note
    activate server
    server->>server: Server saves the new note
    server-->>browser: HTTP 201 Created
    deactivate server

    Note right of browser: JS updates page with the new note without reloading```
