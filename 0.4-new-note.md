```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: User types a note and clicks the Save button

    browser->>browser: JavaScript intercepts the form submission
    browser->>browser: Creates a new note object with the typed content

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note

    activate server

    Note left of server: Server receives the POST request

    server->>server: Server saves the new note
    server-->>browser: HTTP 201 Created
    deactivate server

    Note right of browser: JavaScript updates the page with the new note without reloading
